create template a
(
    string(a),
    number(a,unit=kg),
    range(a,unit=kg,type=error),
    range(a,unit=kg,type=range),
    choice(a,option=[a1,a2,a3]),
    choice(a,optiongroup=[b[b1,b2,b3],c[c1,c2,c3],d[d1,d2,d3]]),
    image(a),    //图片和文件只能存单个，多个去用数组
    file(a),

    table(b).string(a),
    table(b).number(a,unit=kg),
    table(b).range(a,unit=kg,type=error),
    table(b).choice(a,option=[a1,a2,a3]),
    table(b).image(a),
    table(b).file(a),

    array(c).string,
    array(c).number(unit=kg),
    array(c).range(unit=kg,type=error),
    array(c).choice(option=[a1,a2,a3]),
    array(c).image,
    array(c).file,

    array(c).table.string(a),
    array(c).table.number(a,unit=kg),
    array(c).table.range(a,unit=kg,type=error),
    array(c).table.choice(a,option=[a1,a2,a3]),
    array(c).table.image(a),
    array(c).table.file(a),

    array(c).container.string(a),
    array(c).container.number(a,unit=kg),
    array(c).container.range(a,unit=kg,type=error),
    array(c).container.choice(a,option=[a1,a2,a3]),
    array(c).container.image(a),
    array(c).container.file(a),
    array(c).container.table(b).string(a),
    array(c).container.table(b).number(a,unit=kg),
    array(c).container.table(b).range(a,unit=kg,type=error),
    array(c).container.table(b).choice(a,option=[a1,a2,a3]),
    array(c).container.table(b).image(a),
    array(c).container.table(b).file(a),

    array(c).generator.string(a),
    array(c).generator.number(a,unit=kg),
    array(c).generator.range(a,unit=kg,type=error),
    array(c).generator.choice(a,option=[a1,a2,a3]),
    array(c).generator.image(a),
    array(c).generator.file(a),
    array(c).generator.table(b).string(a),
    array(c).generator.table(b).number(a,unit=kg),
    array(c).generator.table(b).range(a,unit=kg,type=error),
    array(c).generator.table(b).choice(a,option=[a1,a2,a3]),
    array(c).generator.table(b).image(a),
    array(c).generator.table(b).file(a),

    container(d).string(a),
    container(d).number(a,unit=kg),
    container(d).range(a,unit=kg,type=error),
    container(d).choice(a,option=[a1,a2,a3]),
    container(d).image(a),
    container(d).file(a),
    container(d).table(b).string(a),
    container(d).table(b).number(a,unit=kg),
    container(d).table(b).range(a,unit=kg,type=error),
    container(d).table(b).choice(a,option=[a1,a2,a3]),
    container(d).table(b).image(a),
    container(d).table(b).file(a),

    generator(e).string(a),
    generator(e).number(a,unit=kg),
    generator(e).range(a,unit=kg,type=error),
    generator(e).choice(a,option=[a1,a2,a3]),
    generator(e).image(a),
    generator(e).file(a),
    generator(e).table(b).string(a),
    generator(e).table(b).number(a,unit=kg),
    generator(e).table(b).range(a,unit=kg,type=error),
    generator(e).table(b).choice(a,option=[a1,a2,a3]),
    generator(e).table(b).image(a),
    generator(e).table(b).file(a)
)