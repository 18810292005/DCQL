create template a
(
    string(a),
    number(a,unit=kg),
    range(a,unit=kg,type=error),
    range(a,unit=kg,type=range),
    choice(a,option=[a1,a2,a3]),
    choice(a,optiongroup=[b[b1,b2,b3],c[c1,c2,c3],d[d1,d2,d3]]),
    image(a),    //图片和文件只能存单个，多个去用数组
    file(a),
    table
    (
        b,
        string(a),
        number(a,unit=kg),
        range(a,unit=kg,type=error),
        range(a,unit=kg,type=range),
        choice(a,option=[a1,a2,a3]),
        image(a),  
        file(a)
    ),
    array(c,string),
    array(c,number(unit=kg)),
    array(c,range(unit=kg,type=error)),
    array(c,choice(option=[a1,a2,a3])),
    array(c,image),
    array(c,file),
    array
    (
        c,
         table
        (
            string(a),
            number(a,unit=kg),
            range(a,unit=kg,type=error),
            range(a,unit=kg,type=range),
            choice(a,option=[a1,a2,a3]),
            image(a),  
            file(a)
        )
    ),
    array
    (
        c,
         container
        (
            string(a),
            number(a,unit=kg),
            range(a,unit=kg,type=error),
            range(a,unit=kg,type=range),
            choice(a,option=[a1,a2,a3]),
            image(a),  
            file(a),
            table
            (
                b,
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a)
            )
        )
    ),
    array
    (
        c,
         generator
        (
            string(a),
            number(a,unit=kg),
            range(a,unit=kg,type=error),
            range(a,unit=kg,type=range),
            choice(a,option=[a1,a2,a3]),
            image(a),  
            file(a),
            table
            (
                b,
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a)
            )
        )
    ),
    container
    (
        d,
        string(a),
        number(a,unit=kg),
        range(a,unit=kg,type=error),
        range(a,unit=kg,type=range),
        choice(a,option=[a1,a2,a3]),
        image(a),  
        file(a),
        table
        (
            b,
            string(a),
            number(a,unit=kg),
            range(a,unit=kg,type=error),
            range(a,unit=kg,type=range),
            choice(a,option=[a1,a2,a3]),
            image(a),  
            file(a)
        ),
        array(c,string),
        array(c,number(unit=kg)),
        array(c,range(unit=kg,type=error)),
        array(c,choice(option=[a1,a2,a3])),
        array(c,image),
        array(c,file),
        array
        (
            c,
            table
            (
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a)
            )
        ),
        array
        (
            c,
            container
            (
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a),
                table
                (
                    b,
                    string(a),
                    number(a,unit=kg),
                    range(a,unit=kg,type=error),
                    range(a,unit=kg,type=range),
                    choice(a,option=[a1,a2,a3]),
                    image(a),  
                    file(a)
                )
            )
        ),
        array
        (
            c,
            generator
            (
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a),
                table
                (
                    b,
                    string(a),
                    number(a,unit=kg),
                    range(a,unit=kg,type=error),
                    range(a,unit=kg,type=range),
                    choice(a,option=[a1,a2,a3]),
                    image(a),  
                    file(a)
                )
            )
        )
    ),
    generator
    (
        e,
        string(a),
        number(a,unit=kg),
        range(a,unit=kg,type=error),
        range(a,unit=kg,type=range),
        choice(a,option=[a1,a2,a3]),
        image(a),  
        file(a),
        table
        (
            b,
            string(a),
            number(a,unit=kg),
            range(a,unit=kg,type=error),
            range(a,unit=kg,type=range),
            choice(a,option=[a1,a2,a3]),
            image(a),  
            file(a)
        )，
        array(c,string),
        array(c,number(unit=kg)),
        array(c,range(unit=kg,type=error)),
        array(c,choice(option=[a1,a2,a3])),
        array(c,image),
        array(c,file),
        array
        (
            c,
            table
            (
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a)
            )
        ),
        array
        (
            c,
            container
            (
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a),
                table
                (
                    b,
                    string(a),
                    number(a,unit=kg),
                    range(a,unit=kg,type=error),
                    range(a,unit=kg,type=range),
                    choice(a,option=[a1,a2,a3]),
                    image(a),  
                    file(a)
                )
            )
        ),
        array
        (
            c,
            generator
            (
                string(a),
                number(a,unit=kg),
                range(a,unit=kg,type=error),
                range(a,unit=kg,type=range),
                choice(a,option=[a1,a2,a3]),
                image(a),  
                file(a),
                table
                (
                    b,
                    string(a),
                    number(a,unit=kg),
                    range(a,unit=kg,type=error),
                    range(a,unit=kg,type=range),
                    choice(a,option=[a1,a2,a3]),
                    image(a),  
                    file(a)
                )
            )
        )
    )
)